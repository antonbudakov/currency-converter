import { CurrencyTitlePipe } from './currency-title.pipe';

describe('CurrencyTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
